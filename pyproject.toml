[project]
name = "dme-dmarc-change"
version = "0.1.0"
description = "DNSMadeEasy DMARC Record Manager - Identify and update non-compliant DMARC records across domains"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests==2.32.5",
]
